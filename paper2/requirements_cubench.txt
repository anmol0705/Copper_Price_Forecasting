# CuBench additional requirements — installed into the EXISTING .venv_corr
# (Python 3.12; do NOT create a fresh venv; do NOT touch the top-level requirements.txt,
# which is Paper 1's frozen environment).
#
# pypbo is not on PyPI; installed from GitHub at a pinned commit for reproducibility.
# Pinned commit SHA (esvhd/pypbo@master, resolved 2026-08-19 via GitHub API):
#   4d723f06498267a2a6280cb9d7d5649348e961d1
#
# DISCREPANCY FROM PLAN (recorded per task instructions): `pip install git+https://...`
# FAILS on this commit — the esvhd/pypbo repo ships no setup.py / pyproject.toml at all
# (confirmed by cloning the pinned SHA and inspecting the tree: only pypbo/, tests/,
# README.md, LICENSE). This is not a transient error; the repo is genuinely not a
# pip-installable Python project. Resolution used (functionally equivalent, still
# pinned to the exact commit above): clone the repo at that SHA and copy the `pypbo/`
# source directory verbatim into `.venv_corr/Lib/site-packages/pypbo`. Reproduce with:
#   git clone https://github.com/esvhd/pypbo.git /tmp/pypbo_pin
#   git -C /tmp/pypbo_pin checkout 4d723f06498267a2a6280cb9d7d5649348e961d1
#   cp -r /tmp/pypbo_pin/pypbo .venv_corr/Lib/site-packages/pypbo
# This confirms Risk R10 in the implementation plan ("pypbo is a single-maintainer
# GitHub repo" / packaging risk) as real, not hypothetical.
# Runtime deps used by pypbo (already present in .venv_corr): joblib, seaborn, statsmodels.

lightgbm>=4.5
xgboost>=2.1
catboost>=1.2
arch>=7.0            # GARCH / GJR-GARCH — statsmodels has no equivalent
shap>=0.46
git+https://github.com/esvhd/pypbo.git@4d723f06498267a2a6280cb9d7d5649348e961d1
dieboldmariano>=1.0  # reference cross-check ONLY, not the primary implementation
torch>=2.0            # CPU wheel only, for the reused LSTM/Transformer
tabulate
